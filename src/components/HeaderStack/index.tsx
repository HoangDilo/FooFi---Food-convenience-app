import {Text, View} from 'react-native';
import React from 'react';
import {ScaledSheet} from 'react-native-size-matters/extend';

const HeaderStack = () => {
  return (
    <View>
      <Text>HeaderStack</Text>
    </View>
  );
};

export default HeaderStack;

const styles = ScaledSheet.create({});
